services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:///./data/rail_planner.db
      JWT_SECRET: change-this-before-production
    volumes:
      - planner_data:/app/data

volumes:
  planner_data:

